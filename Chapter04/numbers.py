# Function range()
for value in range(1, 5):  # 5 is border, so it will return [1-4]
    print(value)

# Creating numbers list
numbers = list(range(1, 6))
print(numbers)

# Creating list with even numbers
even_numbers = list(range(2, 11, 2))  # third argument is step (n + 2)
print(even_numbers)

